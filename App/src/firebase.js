import auth from '@react-native-firebase/auth';
import app from '@react-native-firebase/app';

// RNFirebase auto-initializes when native config files (google-services.json / GoogleService-Info.plist) are present.
// This file exports commonly used firebase modules for convenience.

export { app, auth };
